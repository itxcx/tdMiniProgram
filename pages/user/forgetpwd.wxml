<view class="pages">
	<view class="forgetpwd-form">
		<view class="phone-wrapper">
			<text class="label">手机号码</text>
			<input type="number" bindinput="onPhoneInput" maxlength="11" placeholder="请输入手机号码" placeholder-style="color:#ccc" />
		</view>
		<view class="verification-wrapper">
			<text class="label">验证码</text>
			<input type="number" bindinput="onVerificationInput" maxlength="6" placeholder="请输入验证码" placeholder-style="color:#ccc" />
			<view class="right">
				<text class="left-time" wx:if="{{leftTime >= 0}}">{{leftTime}}s</text>
				<text class="send-btn" bindtap="onSendVerificateCode" wx:else>发送验证码</text>
			</view>
		</view>
	</view>
	<button class="next-btn" bindtap="onNext">下一步</button>
</view>
